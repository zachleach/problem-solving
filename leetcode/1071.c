/**
 * return the largest substring that 'divides' both s1 and s2
 * where t divides s if `s = t + ... + t`
 *
 * step through all substrings in s1
 * check if they are in s2
 * if a substring is found and is larger than the current substring, store it
 * return the largest substring found
 *
 * note that you can compare n characters of two char* with strncmp()
 */
char* gcdOfStrings(char* s1, char* s2) {

}

int main() {

}
